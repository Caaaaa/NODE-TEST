const config = {
    port: 3000,
    database: {
        DATABASE: 'node_koa',
        USERNAME: 'root',
        PASSWORD: 'nodekoa',
        PORT: '3306',
        HOST: 'localhost'
    }
}
module.exports = config
